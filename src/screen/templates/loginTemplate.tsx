import React from 'react';
import {FlexView} from '../atom/styled';
import {VideoBackgrounds} from '../atom/view';
import ButtonGroup from '../organisms/buttonGroup';

export default () => {
  return (
    <FlexView>
      <FlexView />
      <ButtonGroup />
      <VideoBackgrounds />
    </FlexView>
  );
};
